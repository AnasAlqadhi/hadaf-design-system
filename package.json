{
  "name": "hadaf-automation",
  "version": "1.0.0",
  "description": "Build-time automation for Hadaf — fetches RSS, rewrites in Arabic via Gemini Flash, bakes into static JSON",
  "type": "module",
  "private": true,
  "scripts": {
    "fetch": "node scripts/fetch-rewrite.mjs",
    "fetch:dry": "node scripts/fetch-rewrite.mjs --dry-run"
  },
  "dependencies": {
    "fast-xml-parser": "^4.5.0"
  }
}
